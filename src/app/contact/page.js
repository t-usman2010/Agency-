import ContactContent from './ContactContent';

export const metadata = {
  title: 'Contact',
  description:
    'Get in touch with WebNest Studio. Tell us about your project and we\'ll respond within 24 hours with a free consultation.',
};

export default function ContactPage() {
  return <ContactContent />;
}
